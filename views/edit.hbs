<form action="/records/{{ record._id }}?_method=PUT" method="POST">
  <div class="mb-3">
    <label for="name" class="form-label">名稱<span class="text-danger">*</span></label>
    <input type="text" class="form-control" id="name" name="name" value="{{ record.name }}" required>
  </div>
  <div class="mb-3">
    <label for="date" class="form-label">日期<span class="text-danger">*</span></label>
    <input type="date" class="form-control" id="date" name="date" value="{{ record.date }}" required>
  </div>
  <div class="mb-3">
    <label for="categoryName" class="form-label">類別<span class="text-danger">*</span></label>
    <select name="categoryName" id="categoryName">
      <option value="{{ categorySelect }}" selected="selected">{{ categorySelect }}</option>
      {{#each categories}}
      <option value="{{ this.name }}">{{ this.name }}</option>
      {{/each}}
    </select>
  </div>
  <div class="mb-3">
    <label for="amount" class="form-label">金額<span class="text-danger">*</span></label>
    <input type="number" class="form-control" id="amount" name="amount" value="{{ record.amount }}" required>
  </div>


  <button type="submit" class="btn btn-outline-light btn-success">送出</button>
</form>